{
  "name": "kicktires",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static site: what a used car actually costs, checked against NHTSA records.",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && python3 -m http.server 8080 --directory dist"
  },
  "engines": { "node": ">=20" }
}
